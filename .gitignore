# Gradle build artifacts
build/
.gradle/

# Gradle wrapper JAR should be excluded, but scripts and properties should be committed
gradle/wrapper/gradle-wrapper.jar

# Downloaded artifacts (should be downloaded via Gradle)
docker/artifacts/*.jar

# IDE and editor files
.vscode/
.idea/
*.iml
*.ipr
*.iws
.project
.classpath
.settings/

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*~

# Docker Compose override files (but keep main docker-compose.yml)
docker-compose.override.yml
docker-compose.override.yaml
docker-compose.local.yml

# Environment-specific files
.env
.env.local
.env.*.local

# Temporary files
*.tmp
*.temp
*.log
*.swp
*.swo

# Local test configurations (but keep examples)
helm/values-local.yaml
# helm/values-test-local.yaml - keep this as an example

# Backup files
*.bak
*.backup

# JVM crash logs
hs_err_pid*

# Maven (if ever used)
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties

# Kubernetes secrets and sensitive data
secrets/
*.key
*.pem
*.p12
*.jks
*.keystore
*.truststore

# Local Kubernetes configs
kubeconfig
*kubeconfig*

# Helm charts packages
*.tgz

# Local development overrides
docker-compose.local.yml
values-*.local.yaml